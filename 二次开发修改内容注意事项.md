# rv1106-system-NoxKVM CI/CD 变更说明

## 一、概述

本次变更围绕 rv1106-system 固件项目的 CI/CD 流程展开，涉及以下核心改动：

- 新增 GitHub Actions `release.yml` 工作流
- 修改 `build.yml` 为 NAND 单板构建
- 修改 `update_app.sh` 从 COS 下载 app 二进制
- 统一版本号管理流程

---

## 二、Release Workflow (`.github/workflows/release.yml`)

### 触发方式

- push `v*` tag（如 `v0.3.0`）
- 手动 workflow_dispatch（指定版本号）

### 四个 Job

| Job | 作用 | 依赖 |
|---|---|---|
| `build` | NAND 固件构建，上传 3 个 artifact | 无 |
| `release` | 生成 SHA256 校验和，创建 GitHub Release | build |
| `upload-cos` | 上传固件到腾讯 COS（版本化 + latest） | build, release |
| `update-cos-index` | 触发 `cos-index-repo` workflow | build, upload-cos |

### 构建产物

| Artifact | 文件 | 说明 |
|---|---|---|
| OTA-Image | `update_ota.tar` | OTA 升级包 |
| Full-Image | `update.img` | 完整镜像 |
| jetkvm-native-buildkit | `buildkit.tar.zst` | 交叉编译工具包 |

### COS 路径结构

```
noxkvm/firmware/
├── {version}/
│   ├── update_ota.tar
│   ├── update.img
│   └── buildkit.tar.zst
└── latest/
    ├── update_ota.tar
    ├── update.img
    └── buildkit.tar.zst
```

### 需要配置的 GitHub Secrets

| Secret | 用途 |
|---|---|
| `TENCENT_COS_BUCKET` | COS 存储桶名称 |
| `TENCENT_COS_REGION` | COS 区域 |
| `TENCENT_SECRET_ID` | 腾讯云 API SecretId |
| `TENCENT_SECRET_KEY` | 腾讯云 API SecretKey |
| `COS_INDEX_PAT` | 触发 cos-index-repo 的 PAT |

---

## 三、Build Workflow (`.github/workflows/build.yml`)

### 变更内容

- 从 EMMC/SDMMC matrix 改为 NAND 单板构建
- BoardConfig: `BoardConfig-NAND-NONE-RV1106_JETKVM_V2.mk`
- 触发条件：`push`（所有分支）和 `pull_request`

### 构建产物（与 release 一致）

- `update_ota.tar`
- `update.img`
- `buildkit.tar.zst`

---

## 四、App 下载方式变更 (`update_app.sh`)

### 变更前

```bash
SKU="${1:?SKU is required}"
curl -fL "https://api.jetkvm.com/releases/app/latest?sku=${SKU}" -o /tmp/jetkvm_app
```

### 变更后

```bash
APP_URL="${1:-https://dl.acenova.tech/noxkvm/app/latest/jetkvm_app}"
curl -fL "$APP_URL" -o /tmp/jetkvm_app
```

### 原因

NoxKVM 项目（app 端）的 release workflow 将 `jetkvm_app` 二进制上传到 COS：
- 路径：`noxkvm/app/{version}/jetkvm_app` + `noxkvm/app/latest/jetkvm_app`
- CDN 域名：`dl.acenova.tech`

app 二进制是通用的，不区分 SKU，直接从 COS 下载最新版。

---

## 五、SKU 机制说明

### SKU 的工作原理

SKU 是**固件层面**的概念，不是 app 编译层面的：

1. BoardConfig 中通过 `RK_SKU` 定义（如 EMMC: `jetkvm-v2`，SDMMC: `jetkvm-v2-sdmmc`）
2. `build.sh` 打包 rootfs 时写入 `/etc/jetkvm-sku`（`build.sh:1433-1434`）
3. app 运行时读取该文件（`NoxKVM/hw.go:86-95` 的 `GetDeviceSKU()`）
4. 用于 OTA 查询参数（`?sku=`）和云端上报（`X-Device-SKU` header）

### NAND 的 SKU 处理

经过讨论，NAND **不设置专用 SKU**：

- NAND BoardConfig 中不定义 `RK_SKU`
- 固件中不会生成 `/etc/jetkvm-sku` 文件
- app 运行时读不到该文件，会走默认值 `"jetkvm-v2"`（`hw.go:90`）

**原因**：目前 app 二进制是通用的，不按 SKU 分发不同版本。如果未来需要区分 NAND 的 OTA 行为，再添加 `RK_SKU=jetkvm-v2-nand` 以及对应的脚本映射即可。

### 各板型 SKU 现状

| 板型 | BoardConfig | `RK_SKU` | `/etc/jetkvm-sku` |
|---|---|---|---|
| EMMC | `BoardConfig-EMMC-NONE-RV1106_JETKVM_V2.mk` | `jetkvm-v2` | ✅ 写入 |
| SDMMC | `BoardConfig-SDMMC-NONE-RV1106_JETKVM_V2.mk` | `jetkvm-v2-sdmmc` | ✅ 写入 |
| NAND | `BoardConfig-NAND-NONE-RV1106_JETKVM_V2.mk` | 未设置 | ❌ app 默认 `jetkvm-v2` |

---

## 六、版本号管理

### 三个版本号来源

| 来源 | 版本号 | 用途 |
|---|---|---|
| `VERSION` 文件 | `0.2.9` | 日常开发基线版本 |
| CI `build.yml` | `0.2.9-dev202501151234`（dev 分支） | 每次构建的标识 |
| CI `release.yml` | tag 或手动输入（如 `0.3.0`） | 正式发布版本 |

### 版本号使用场景对比

| 场景 | 版本号来源 | 是否写入固件 | 是否 commit 回仓库 |
|---|---|---|---|
| 本地 `make build`（dev 分支） | `VERSION` 文件 + `+时间戳` | ✅ 写入 `rootfs/version` | ❌ |
| 本地 `make build`（main 分支） | `VERSION` 文件原值 | ✅ 写入 `rootfs/version` | ❌ |
| CI `build.yml` | `VERSION` 文件 + 分支时间戳 | ✅ 写入 `rootfs/version` | ❌ |
| CI `release.yml`（tag 触发） | git tag（`v0.3.0` → `0.3.0`） | ✅ 写入 `rootfs/version` | ❌ |
| CI `release.yml`（手动触发） | workflow_dispatch 输入值 | ✅ 写入 `rootfs/version` | ❌ |
| `make bump-version` | 交互输入新版本号 | ❌ | ✅ 写入 `VERSION` → commit → push |

### 版本号流程

```
日常开发：VERSION 文件 → build.yml 读取 → dev 分支追加时间戳
                                              ↓
发布：    git tag v0.3.0 → release.yml 提取 → 写入 VERSION（CI 内）→ 构建 + 发布
                                              ↓
发布后：  make bump-version → 交互输入新版本 → 写入 VERSION → git commit → git push
```

### 本地发布流程（通过 Makefile）

项目有两套发布路径：

**1. CI 自动发布（release.yml）** — tag 触发或手动触发，走 GitHub Actions。

**2. 本地手动发布（Makefile）** — 通过 `scripts/` 脚本，有三种模式：

| 命令 | 版本号 | 签名 | 上传目标 | tag 格式 |
|---|---|---|---|---|
| `make dev_release` | `{VERSION}-dev{时间戳}` | 无（`--unsigned`） | R2 + GitHub（prerelease） | `release/v{版本}` |
| `make release SIGNING_KEY_FPR=<指纹>` | `VERSION` 文件值 | GPG YubiKey 签名 | R2 + GitHub | `release/v{版本}` |
| `make release_dry_run SIGNING_KEY_FPR=<指纹>` | `VERSION` 文件值 | 签名但不上传 | 无（仅验证） | 无 |

本地发布前会检查：
- 工作区是否干净（`git status --porcelain`）
- 当前分支是否与 `origin/dev` 同步（不同步则提示确认）
- R2 和 GitHub 上是否已存在同版本号
- 正式发布还会检查是否有对应的 dev release

### 关键点

- release.yml 会覆盖 CI 内的 `VERSION` 文件，但**不会 commit 回仓库**
- 发布后需要手动执行 `make bump-version` 更新 `VERSION` 文件
- `build.sh` 打包 rootfs 时，如果 `BUILD_VERSION` 环境变量未设，会用 `VERSION` + 时间戳作为 fallback
- 本地发布的 tag 格式是 `release/v{版本}`，CI 发布的 tag 格式是 `v{版本}`，两者的 tag 触发的 workflow 不同
- `bump-version` 默认递增 patch 版本（`0.2.9` → `0.3.0`），也支持手动输入任意 semver 版本号

---

## 七、脚本体系（`scripts/` 目录）

### 脚本与 Makefile 的关系

| make 命令 | 调用的脚本 |
|---|---|
| `make build` | `build_system.sh` |
| `make flash` | `flash_system.sh`（默认先 `--build`） |
| `make test` | `check_device.sh` → `flash_system.sh` → `run_e2e_tests.sh` |
| `make dev_release` | `build_system.sh` → `release_r2.sh --unsigned` → `release_github.sh --prerelease` |
| `make release` | `build_system.sh` → `release_r2.sh --signing-key` → `release_github.sh` |
| `make bump-version` | 直接操作 `VERSION` 文件 + git |

### 各脚本功能

| 脚本 | 用途 |
|---|---|
| `common.sh` | 公共库：颜色输出、SSH 连接、SKU 映射、产物路径 |
| `build_system.sh` | 本地构建固件 |
| `flash_system.sh` | SSH + `rk_ota` 刷写固件到设备 |
| `check_device.sh` | 检查设备网络连通性 |
| `run_e2e_tests.sh` | 端到端测试 |
| `release_github.sh` | 创建 GitHub Release |
| `release_r2.sh` | 上传固件到 Cloudflare R2 |

### SKU 映射现状（`scripts/common.sh`）

脚本中只定义了 EMMC/SDMMC 的映射，没有 NAND。NAND 设备本地开发时：
- `build_system.sh` 只能构建 EMMC/SDMMC
- `flash_system.sh` 只能刷写 EMMC/SDMMC
- 如需支持 NAND，需在 `common.sh` 中添加 `NAND_SKU` 和 `NAND_BOARD_CONFIG` 映射

---

## 八、参考项目对比（NoxKVM）

### NoxKVM release workflow 结构

| Job | 是否适用固件项目 |
|---|---|
| build（Go app 编译） | ✅ 已适配为固件构建 |
| release（GitHub Release） | ✅ 已适配 |
| upload-cos | ✅ 已适配 |
| deploy-pages | ❌ 不需要（OTA metadata） |
| update-cos-index | ✅ 已适配 |

### 关键差异

| 对比项 | NoxKVM（app） | rv1106-system（固件） |
|---|---|---|
| GPG 签名 | ✅ | ❌ 不需要 |
| OTA metadata | ✅ | ❌ 不需要 |
| GitHub Pages | ✅ | ❌ 不需要 |
| 构建产物 | 单个 `jetkvm_app` | `update.img` + `update_ota.tar` + `buildkit.tar.zst` |
| COS 路径 | `noxkvm/app/` | `noxkvm/firmware/` |
| SKU 处理 | 不涉及 | 固件层面，通过 `/etc/jetkvm-sku` |

---

## 九、注意事项

1. **build.yml 触发范围**：当前 `on: push:` 无分支限制，建议限制为 `branches: [main, dev]` 以节省 Actions 额度
2. **build.yml 权限**：只上传 artifact，`contents: read` 即可，无需 `contents: write`
3. **`update_app.sh` 不接受 SKU 参数**：任何调用它的地方都不应传入 SKU
4. **构建产物无板型后缀**：`update.img`、`update_ota.tar` 不区分板型，重命名发生在发布脚本层面
5. **版本号发布后需手动 bump**：release.yml 不会自动更新仓库中的 `VERSION` 文件
6. **CI 发布与本地发布共存**：两种路径独立运作。CI 用 `v*` tag（`v0.3.0`），本地用 `release/v*` tag（`release/v0.2.9-dev202501151234`）。release.yml 只监听 `v*`，不会被本地发布的 `release/v*` tag 触发
7. **`VERSION` 文件是开发基线，不是发布版本**：`VERSION` 文件里的值（如 `0.2.9`）只用于本地和 CI build 的 dev 标识。正式发布版本号完全由 git tag 或手动输入决定，两者独立互不干扰

---

## 十、Fork 相对 Upstream 的改动清单

上游仓库：`jetkvm/rv1106-system`（dev 分支）

### 10.1 被删除的文件

| 文件 | 说明 |
|---|---|
| `output/.gitkeep` | 上游存在，fork 中被删除。`output/` 目录在 `.gitignore` 中，构建时自动生成 |

### 10.2 新增文件

| 文件 | 说明 |
|---|---|
| `.github/workflows/release.yml` | 新增 release workflow（4 jobs） |
| `project/cfg/BoardConfig_IPC/BoardConfig-NAND-NONE-RV1106_JETKVM_V2.mk` | NAND 板级配置（112 行），定义 SPI NAND 启动、UBIFS 文件系统、A/B 分区 |
| `nox_private_files/network_port_leds/S90network_led` | 网口 LED 启动脚本（init.d） |
| `nox_private_files/network_port_leds/network_led.sh` | 网口 LED 控制主程序（GPIO40 常亮/ GPIO41 闪烁） |
| `nox_private_files/tailscale/S99tailscaled` | Tailscale 启动脚本（init.d） |
| `nox_private_files/tailscale/tailscale` | Tailscale CLI ARM 二进制 |
| `nox_private_files/tailscale/tailscaled` | Tailscale daemon ARM 二进制 |
| `nox_private_files/tailscale/.DS_Store` | macOS 元数据文件（应被 .gitignore 忽略） |
| `project/app/jetkvm/S90network_led` | 同 nox_private_files 版本，参与编译打包 |
| `project/app/jetkvm/S99tailscaled` | 同 nox_private_files 版本，参与编译打包 |
| `project/app/jetkvm/network_led.sh` | 同 nox_private_files 版本，参与编译打包 |
| `project/app/jetkvm/tailscale` | 同 nox_private_files 版本，参与编译打包 |
| `project/app/jetkvm/tailscaled` | 同 nox_private_files 版本，参与编译打包 |
| `sysdrv/tools/board/rootfs/etc/ssl/certs/ca-certificates.crt` | 新增 CA 根证书包（+3610 行），解决设备上 TLS 证书验证问题 |

### 10.3 修改的文件

| 文件 | 改动内容 | 注意事项 |
|---|---|---|
| `.github/workflows/build.yml` | EMMC/SDMMC matrix → NAND 单板构建；`update_app.sh` 调用去掉 SKU 参数 | ⚠️ 上游仍是 matrix 构建，合回上游时需协调 |
| `update_app.sh` | 从 `api.jetkvm.com?sku=` 改为 COS 下载 `dl.acenova.tech/noxkvm/app/latest/jetkvm_app` | ⚠️ 依赖 NoxKVM 项目的 COS 发布流程，合回上游需讨论 |
| `.gitignore` | 新增 `.DS_Store` | 无风险 |
| `README.md` | 编译示例改为 NAND BoardConfig | ⚠️ 上游 README 仍是 EMMC，合回需协调 |
| `project/app/jetkvm/Makefile` | 新增安装 Tailscale 二进制、LED 控制脚本、init 脚本到固件 | ⚠️ 新增了 Tailscale ARM 二进制（~60MB），会增大固件体积 |
| `sysdrv/source/kernel/arch/arm/boot/dts/rv1106-jetkvm-v2.dtsi` | bootargs 增加 NAND/UBIFS 启动参数；新增 LED 节点（GPIO3 PC6，activity 触发） | ⚠️ bootargs 改动会影响 EMMC/SDMMC 启动，需确认上游是否有独立的 dtsi |
| `sysdrv/source/kernel/arch/arm/boot/dts/rv1106g-jetkvm-v2.dts` | 从简短的 eMMC 配置改为完整的 NAND 硬件定义：SPI NAND、SD 卡、以太网、USB、I2C（EEPROM+触摸屏）、SPI（ST7789V LCD）、PWM 背光、UART、RTC、看门狗 | ⚠️ 大量改动，基本是完整的硬件外设重定义，合回上游需仔细对比 |
| `sysdrv/source/kernel/arch/arm/configs/rv1106-jetkvm-v2_defconfig` | 新增 `CONFIG_CMD_MMC=y`、UBIFS LZO/ZLIB 压缩支持 | ⚠️ UBIFS 相关配置是 NAND 必需的，合回需确认是否影响 EMMC/SDMMC |
| `sysdrv/tools/board/rootfs/etc/fstab` | 新增 `/dev/mmcblk1p1 /mnt/sdcard vfat defaults 0 0` 自动挂载 SD 卡 | 低风险，不影响其他板型 |
| `sysdrv/tools/board/rootfs/etc/profile.d/RkEnv.sh` | PATH 中新增 `/userdata/tailscale`，支持直接运行 tailscale CLI | 低风险，不影响其他板型 |
| `project/app/jetkvm/jetkvm/bin/jetkvm_app` | 二进制更新（上游 → v0.5.9），体积从 38MB 缩小到 31MB | ⚠️ 非编译产物，直接替换的预编译二进制 |
| `install.sh` | 仅文件权限 644 → 755，无内容变更 | 无风险 |

### 10.4 重复文件（两处存放）

网络 LED 和 Tailscale 脚本/二进制同时存在于 `nox_private_files/` 和 `project/app/jetkvm/` 两处，内容完全相同：

| 文件 | `nox_private_files/` | `project/app/jetkvm/` |
|---|---|---|
| `S90network_led` | `network_port_leds/` | 直接 |
| `network_led.sh` | `network_port_leds/` | 直接 |
| `S99tailscaled` | `tailscale/` | 直接 |
| `tailscale`（二进制） | `tailscale/` | 直接 |
| `tailscaled`（二进制） | `tailscale/` | 直接 |

`nox_private_files/` 是私有备份，`project/app/jetkvm/` 是实际参与编译打包的。修改时需同步两处。

### 10.5 未被上游覆盖的改动（已落后）

Fork 没有上游 `dev` 的额外提交（`git log HEAD..upstream/dev` 为空），说明 fork 是基于上游 dev 的最新状态。

### 10.6 合回上游时的注意事项

1. **NAND BoardConfig** 是全新文件，不影响上游 EMMC/SDMMC 流程
2. **设备树改动**（`.dtsi` + `.dts`）是最大的风险点——bootargs 里硬编码了 NAND/UBIFS 启动参数，如果上游仍用 EMMC，会导致启动失败。建议上游使用独立的 NAND DTS 文件
3. **kernel defconfig** 增加了 UBIFS 配置，这些配置对 EMMC/SDMMC 无副作用（只是启用了额外的文件系统模块）
4. **CI workflow 改动**只影响本 fork，不影响上游
5. **Tailscale 二进制**（~60MB）打包进固件会显著增大体积，合回上游需讨论是否作为可选组件
6. **CA 证书**是纯新增，对所有板型都有益，建议上游采纳
7. **SD 卡自动挂载**和 **Tailscale PATH** 是 rootfs 层面的小改动，对所有板型通用
